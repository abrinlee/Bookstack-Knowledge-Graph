/**
 * Client-side Configuration Template
 *
 * SETUP INSTRUCTIONS:
 * 1. Copy this file to config.js in the same directory:
 *    cp config.example.js config.js
 *
 * 2. Edit config.js with your actual BookStack URL
 *
 * 3. Never commit config.js to version control
 *
 * SECURITY NOTE:
 * - This file is loaded by the browser and is NOT secret
 * - Never put API tokens or credentials here
 * - Only put values the client needs (like base URL)
 */

const CLIENT_CONFIG = {
    // BookStack base URL (used for opening pages in new tabs)
    // Example: 'http://192.168.0.94' or 'https://bookstack.example.com'
    bookstackBaseUrl: 'YOUR_BOOKSTACK_URL_HERE'
};
